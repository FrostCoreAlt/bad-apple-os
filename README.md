# bad-apple-os
Operating system strictly designed to play the Bad Apple video
# Compiling
Type ```make```
To run under QEMU, type ``make run``, an additional debug run can be executed with ```make debug``` where you can attach your debugger remotely.
